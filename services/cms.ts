
/**
 * CMS SERVICE - MODE LOCAL UNIQUEMENT
 */
export const cmsService = {
  getTranslations: async (lang: string) => null,
  getLoans: async (lang: string) => [],
  getPosts: async (lang: string) => []
};
